# Kotlin Feature Environment Variables

# Build Arguments
# ===============

# Enable Kotlin installation
INCLUDE_KOTLIN=false

# Enable Kotlin development tools (ktlint, detekt, kotlin-language-server)
INCLUDE_KOTLIN_DEV=false

# Kotlin version (e.g., 2.3.0, 2.1.0)
KOTLIN_VERSION=2.3.0

# Runtime Environment Variables
# =============================

# Kotlin paths
KOTLIN_HOME=/opt/kotlin
KOTLIN_NATIVE_HOME=/opt/kotlin-native

# Cache directory
KOTLIN_CACHE_DIR=/cache/kotlin

# Notes
# =====
# - Java is automatically installed when INCLUDE_KOTLIN=true
# - Kotlin/Native is architecture-specific (amd64, arm64)
# - For Gradle projects, use ./gradlew instead of gradle for consistency
# - ktlint and detekt are installed with INCLUDE_KOTLIN_DEV=true
# - kotlin-language-server provides LSP support for IDEs

# Architecture Considerations
# ===========================
# Kotlin itself runs on the JVM and works on both amd64 and arm64.
# For pure Kotlin/JVM development, arm64 works perfectly fine.
# For Kotlin/Native, binaries are architecture-specific.
#
# IMPORTANT: For Android development with Kotlin, see android.env for
# critical architecture requirements. Android SDK tools (adb, emulator)
# are x86_64 only and require emulation on arm64 hosts.
#
# See also: examples/mobile/README.md for complete setup guide.
