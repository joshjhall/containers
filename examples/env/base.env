# Base Container Environment Variables
# These apply to all container contexts

# Runtime Environment Variables
# =============================
# Note: Build arguments like BASE_IMAGE, USERNAME, USER_UID, USER_GID
# are set in the Dockerfile or docker-compose.yml, not here.
# This file only contains runtime environment variables.

# Timezone configuration
TZ=UTC

# Terminal settings
TERM=xterm-256color
COLORTERM=truecolor

# History settings
HISTSIZE=10000
HISTFILESIZE=20000
HISTCONTROL=ignoreboth:erasedups

# Less pager settings
LESS=-R

# Prompt directory trimming
PROMPT_DIRTRIM=3

# Resource Limits (Security)
# ===========================
# File descriptor limit (default: 4096)
# Prevents resource exhaustion from file descriptor leaks
# FILE_DESCRIPTOR_LIMIT=4096

# Max user processes (default: 2048)
# Prevents fork bombs and process exhaustion
# MAX_USER_PROCESSES=2048

# Core dump size (default: 0 = disabled)
# Set to 0 for security, or 'unlimited' for debugging
# CORE_DUMP_SIZE=0
