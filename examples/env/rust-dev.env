# Rust Development Tools Feature Environment Variables

# Build Arguments
# ===============

# Enable Rust development tools installation (requires Rust)
INCLUDE_RUST_DEV=false

# Runtime Environment Variables
# =============================

# sccache configuration
RUSTC_WRAPPER=sccache
SCCACHE_DIR=$HOME/.cache/sccache
SCCACHE_CACHE_SIZE=10G

# Tree-sitter configuration
TREE_SITTER_DIR=$HOME/.tree-sitter
TREE_SITTER_GRAMMAR_DIR=$HOME/.tree-sitter/grammars

# Parser directories
TREE_SITTER_PARSER_INCLUDE_DIR=$HOME/.tree-sitter/parsers
TREE_SITTER_RUNTIME_DIR=/usr/local/lib/tree-sitter

# Language paths (for custom grammars)
TREE_SITTER_LANGUAGE_PATH=$HOME/.tree-sitter/languages

# Compilation options
TREE_SITTER_THREADS=0  # 0 = use all available cores
TREE_SITTER_OPTIMIZE=true

# Cargo watch configuration
CARGO_WATCH_CLEAR=true
CARGO_WATCH_NOTIFY=false

# Bacon configuration
BACON_JOBS=0  # 0 = use all available cores
BACON_PREFS=$HOME/.config/bacon/prefs.toml
