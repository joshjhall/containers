# Python Feature Environment Variables
#
# NOTE: Path examples shown use $HOME to work with any username.
# In practice, tools use $HOME automatically. Only set these if you need
# to override defaults, and use $HOME instead of hardcoded paths.

# Build Arguments (set in Dockerfile/docker-compose.yml, NOT here)
# ================================================================
# INCLUDE_PYTHON=true
# PYTHON_VERSION=3.14.0  # Python version to install from source

# Runtime Environment Variables
# =============================

# Poetry configuration
POETRY_VIRTUALENVS_IN_PROJECT=true
POETRY_NO_INTERACTION=1
POETRY_VIRTUALENVS_CREATE=true
# POETRY_VIRTUALENVS_PATH=$HOME/.cache/pypoetry/virtualenvs  # Default, usually not needed

# Python environment
PYTHONUNBUFFERED=1
PYTHONDONTWRITEBYTECODE=1

# pip configuration
PIP_NO_CACHE_DIR=1
PIP_DISABLE_PIP_VERSION_CHECK=1
PIP_DEFAULT_TIMEOUT=100