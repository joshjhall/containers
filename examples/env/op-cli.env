# 1Password CLI Feature Environment Variables

# Build Arguments
# ===============

# Enable 1Password CLI installation
INCLUDE_OP=false

# Runtime Environment Variables
# =============================

# 1Password configuration
OP_ACCOUNT=
OP_SESSION=
OP_VAULT=
OP_DEVICE=

# 1Password behavior
OP_BIOMETRIC_UNLOCK_ENABLED=false
OP_CACHE_TIMEOUT=300
OP_FORMAT=json

# 1Password paths
OP_CONFIG_DIR=$HOME/.config/op
OP_DATA_DIR=$HOME/.local/share/op
OP_LOG_FILE=/tmp/op.log

# Security options
OP_DEBUG=false
OP_TRACE=false

# Integration settings
OP_MANAGED_VARIABLES=  # Comma-separated list of env vars managed by 1Password

# Automatic Secret Loading (OP_*_REF convention)
# ================================================
# Any variable matching OP_<NAME>_REF is resolved from 1Password
# and exported as <NAME> on shell init and container startup.
# Direct env vars always win (if <NAME> is already set, OP ref is skipped).
#
# Service account token (required for auto-loading)
# Create at: https://developer.1password.com/docs/service-accounts/
OP_SERVICE_ACCOUNT_TOKEN=

# 1Password references — format: op://vault-name/item-name/field-name
# Built-in examples:
OP_GITHUB_TOKEN_REF=
OP_GITLAB_TOKEN_REF=

# MCP server API keys:
# OP_KAGI_API_KEY_REF=op://Vault/Kagi/api-key
# OP_BRAVE_API_KEY_REF=op://Vault/Brave-Search/api-key
# OP_SENTRY_ACCESS_TOKEN_REF=op://Vault/Sentry/token
# OP_PERPLEXITY_API_KEY_REF=op://Vault/Perplexity/api-key

# Custom project secrets (add your own):
# OP_MY_PROJECT_SECRET_REF=op://Vault/Item/field
