# Plugin count fixture — README.md

Synthetic stand-in for the repo README. The only thing the doc-drift scan
reads from a doc is one line matching its narrow count regex, so a fixture
doc is one line of prose plus enough context to look like the real thing.

The container installs all 7 core plugins on first startup.
