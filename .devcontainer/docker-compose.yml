services:
  devcontainer:
    build:
      context: .. # Containers root
      dockerfile: Dockerfile
      args:
        BASE_IMAGE: mcr.microsoft.com/devcontainers/base:trixie
        PROJECT_NAME: containers
        USERNAME: vscode
        WORKING_DIR: /workspace/containers
        # Enable passwordless sudo for development (allows entrypoint to fix Docker socket)
        ENABLE_PASSWORDLESS_SUDO: "true"
        # Main dev tools
        INCLUDE_DEV_TOOLS: "true"
        # Node.js (required for MCP servers and bash-language-server)
        INCLUDE_NODE: "true"
        # Additional dev tools
        INCLUDE_OP: "true"
        INCLUDE_DOCKER: "true"
        # Rust needed for stibbons (project scaffolding CLI) and luggage (containers runtime and install management)
        INCLUDE_RUST_DEV: "true"
      cache_from:
        - type=local,src=/tmp/.buildx-cache
      cache_to:
        - type=local,dest=/tmp/.buildx-cache,mode=max
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ..:/workspace/containers
    env_file:
      - ../.env
    environment:
      # Default MCPs (unset = filesystem + figma-desktop)
      - CLAUDE_MCPS=filesystem
      # Core plugins (unset = all 11 defaults)
      # - CLAUDE_PLUGINS=commit-commands,context7,code-review,pr-review-toolkit,feature-dev,hookify,claude-code-setup
      # Skills (unset = all 13 static template skills + container-environment)
      # - CLAUDE_SKILLS=git-workflow,code-quality,shell-scripting,testing-patterns,development-workflow,skill-authoring,agent-authoring,documentation-authoring,next-issue,next-issue-ship,codebase-audit,error-handling,memory-conventions
      # Agents (unset = all 11 agents)
      # - CLAUDE_AGENTS=debugger,code-reviewer,test-writer,refactorer,audit-code-health,audit-security,audit-test-gaps,audit-architecture,audit-docs,audit-ai-config,issue-writer
      # Default memory path
      - MEMORY_FILE_PATH=/workspace/containers/.claude/memory
    # Use tini as init system for proper zombie process reaping
    # This is a belt-and-suspenders approach - the Dockerfile also includes tini,
    # but init: true ensures proper PID 1 behavior even if command overrides entrypoint
    init: true
    # Required for bindfs FUSE overlay (fixes macOS VirtioFS permission issues)
    cap_add:
      - SYS_ADMIN
    devices:
      - /dev/fuse
    # Entrypoint handles Docker socket permissions via sudo (no root needed)
    # devcontainer.json sets overrideCommand: false so our ENTRYPOINT runs
    command: [ "sleep", "infinity" ]
    # Health check configuration (inherits from Dockerfile)
    # Override if needed: healthcheck: {test: ["CMD", "healthcheck", "--verbose"]}
    networks:
      - containers-network

networks:
  containers-network:
    driver: bridge
