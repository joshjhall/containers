name: git-workflow
version: "1.0"

labels: []

required_tools:
  - name: git
    purpose: Commit, branch, and PR operations
    install_hint: "Included in base image"

required_permissions: []

required_mcps: []
