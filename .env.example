# Container Build System Environment Variables
# Copy this file to .env and update with your values

# 1Password Integration Configuration
# Copy this file to .env and fill in your values

# Required: Your 1Password service account token
# Get one at: https://developer.1password.com/docs/service-accounts/
# OP_SERVICE_ACCOUNT_TOKEN=ops_your_token_here

# Optional: Your 1Password account URL (if different from default)
# OP_ACCOUNT=foo.1password.com

# Optional: Specify which 1Password vault to use
# If not set, will try: Development, Private, Personal (in that order)
# OP_VAULT=Development

# Optional: Custom names for items in 1Password
# If not set, the script will try common default names
# GIT_SIGNING_KEY_ITEM="your-git-signing-key-item-name"
# GIT_CONFIG_ITEM="your-git-config-item-name"

# GitHub CLI Authentication (via 1Password)
# Optional: Vault and item name for GitHub token in 1Password
# If not set, defaults to "Development" vault and "GitHub Development Token" item
# GH_TOKEN_VAULT=Development
# GH_TOKEN_ITEM=GitHub Development Token

# Alternative: GitHub Personal Access Token (for version checking)
# Can also be stored in 1Password (recommended) instead of .env
# Create at: https://github.com/settings/tokens
# Required scopes: repo, workflow, read:org (for gh CLI)
# GITHUB_TOKEN=ghp_your_token_here

# =============================================================================
# CLAUDE CODE AUTHENTICATION
# =============================================================================
# Claude Code CLI requires interactive authentication.
# Environment variables do NOT work with the Claude Code CLI.
#
# To install plugins:
#   1. Run 'claude' inside the container and authenticate when prompted
#   2. Close Claude client (Ctrl+C)
#   3. Run 'claude-setup' to install plugins
#   4. Restart Claude if needed
